---
description: REST 서비스 규칙(Fastify/캐시/경계/성능)
globs:
  - src/**/*.ts
---

- 응답 캐시 헤더(ETag/Cache-Control) 일관 적용.
- 요청 로깅: pino + requestId, 민감정보 제외.
- 경계: 유효성 검증(zod/typebox), 에러 코드 표준.
- 성능 목표: p95 ≤ 50ms(캐시 히트 기준), 바운디드 컨커런시.
- 레이트리밋: Fetch와 분리, X-RateLimit-* 일관 관리.
