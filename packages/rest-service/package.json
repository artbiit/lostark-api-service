{
  "name": "@lostark/rest-api",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "main": "./src/server.ts",
  "types": "./src/server.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.0",
    "@fastify/helmet": "^11.0.0",
    "@fastify/rate-limit": "^9.0.0",
    "@lostark/data-service": "workspace:*",
    "@lostark/shared": "workspace:*",
    "fastify": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
