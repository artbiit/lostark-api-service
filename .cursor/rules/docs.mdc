---
description: 문서/Docs 규칙(구조/중복/인덱스/ENV 참조)
globs:
  - Docs/**/*.md
---

- 문서는 카테고리 기반 구조를 유지하고, 변경 시 같은 레벨의 Index.md를 동기화합니다.
- 존재하지 않는 파일/디렉터리를 문서화하지 않습니다. 파일 구조는 실제와 동일해야 합니다.
- 환경변수 상세 설명은 `.env.example`에만 존재합니다. Docs에는 참조 링크만 둡니다.
- 대용량 JSON/로그/스냅샷은 `cache/**`에 저장하며 문서에 인라인하지 않습니다.
